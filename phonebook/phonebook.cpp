#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;
bool solution(vector<string> phone_book);

int main() {

    return 0;
}

bool solution(vector<string> phone_book) {
    bool answer = true;
    
    return answer;
}